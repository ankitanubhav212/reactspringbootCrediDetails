export const processData = (data) => {
    data.forEach(element => {
        if(element.sex === 1) {
            element.sex = "male"
        } else {
            element.sex = "female"
        }

        switch(e)
    });
}